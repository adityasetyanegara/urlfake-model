# urlfake-model
URLFake is a machine learning project for phishing URL detection. It compares the performance of Random Forest, XGBoost, and LightGBM models using URL-based features. Models are evaluated with metrics such as Accuracy, Precision, Recall, F1-Score, and ROC-AUC to identify the most effective approach.
